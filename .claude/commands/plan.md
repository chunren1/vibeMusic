# /plan — 规划模式

请阅读我提供的 Spec 文件：$ARGUMENTS

分析当前代码库，找出需要修改的文件。

列出详细的实施步骤：
1. 每步包含文件路径 + 函数签名 + 修改内容
2. 标注潜在风险（数据库迁移、搜索同步、API 兼容性）
3. 如果涉及数据表变更，必须列出 SQL 变更

验证要求：
- 每完成一步，运行 `cd vibeMusic-backend && mvn compile -DskipTests -q` 确认编译
- 如果修改涉及推荐/搜索/历史，必须列出受影响模块

输出格式：
```markdown
## 实施计划

### 子任务1: xxx
- 文件: 
- 修改: 
- 风险: 
- 验证: 

### 子任务2: xxx
...
```

❗ 绝对不要修改任何代码，只输出计划。等待我的确认。
