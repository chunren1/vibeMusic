# vibeMusic 自动部署流水线
# 触发: 手动触发 / 推送 tag (v*.*.*)
name: Deploy

on:
  workflow_dispatch:  # 手动触发
  push:
    tags:
      - 'v*'          # 推送 tag 如 v1.0.0 时自动部署

permissions:
  contents: read

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

# NOTE: tests live in ci.yml (backend / frontend / gateway / infra-lint).
# This workflow is artifact-build only; do NOT re-add test or compose-lint jobs here.
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: vibemusic-web/package-lock.json

      - name: Build Backend JAR
        run: cd vibeMusic-backend && mvn package -DskipTests -B

      - name: Build Frontend
        run: cd vibemusic-web && npm ci && npm run build

      # 注意：此处不执行 `docker compose build`。
      # 原因：musicapi/Dockerfile 的 `COPY server.js config.js ./` 依赖 gitignore 的
      # config.js（Cookie 配置），全新 checkout 下该文件不存在，构建必然失败。
      # 修复 Dockerfile（改为 COPY config.example.js 或运行时注入）由并行任务负责；
      # 修复后可在下方恢复 docker compose build 步骤。
      #
      # 真实部署需配置目标（SSH 主机 / 镜像仓库 / 云平台），并注入 secrets：
      #   - 镜像方式：docker build + docker push 到 registry，目标机 docker pull + up
      #   - 产物方式：将下方 jar + dist artifact 上传到目标机后启动
      # 当前阶段仅构建并上传产物，供后续部署流程消费。

      - name: Upload Backend JAR
        uses: actions/upload-artifact@v4
        with:
          name: backend-jar
          path: vibeMusic-backend/target/*.jar
          retention-days: 7

      - name: Upload Frontend dist
        uses: actions/upload-artifact@v4
        with:
          name: frontend-dist
          path: vibemusic-web/dist/
          retention-days: 7

      # 示例：镜像推送与远程部署（需配置 secrets.RSA / REGISTRY）
      # - name: Build & push images
      #   run: |
      #     echo "${{ secrets.REGISTRY_PASSWORD }}" | docker login ${{ secrets.REGISTRY_URL }} -u ${{ secrets.REGISTRY_USERNAME }} --password-stdin
      #     docker compose build backend frontend
      #     docker compose push
