<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vibemusic.mapper.PlayHistoryMapper">

    <!--
        删除旧播放记录，只保留每个用户最近 N 条
        使用 ROW_NUMBER() 窗口函数，单次扫描，避免 NOT IN 双重子查询的性能问题
    -->
    <delete id="deleteOldByUserId">
        DELETE p FROM play_history p
        JOIN (
            SELECT id, ROW_NUMBER() OVER (ORDER BY played_at DESC) AS rn
            FROM play_history
            WHERE user_id = #{userId}
        ) AS ranked ON p.id = ranked.id
        WHERE ranked.rn > #{keep}
    </delete>

</mapper>
