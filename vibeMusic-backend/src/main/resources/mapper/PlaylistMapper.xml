<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vibemusic.mapper.PlaylistMapper">

    <!--
        一次查询获取用户所有歌单及统计数据（歌曲数 + 最新封面）

        索引依赖：
        - idx_user_created (user_id, created_at) → 主查询过滤 + 排序
        - idx_pl_added (playlist_id, added_at)   → 子查询定位最新封面，一次索引 seek 即返回

        子查询为关联子查询，每个 playlist 触发一次：
            ORDER BY added_at DESC LIMIT 1
        因为 idx_pl_added 前缀是 playlist_id，MySQL 直接定位到该歌单的索引段，
        按 added_at DESC 读取第一条即可，等价于一次覆盖索引的 range scan。
        每个用户通常 ≤20 个歌单，总开销可忽略。
    -->
    <select id="listPlaylistsWithStats" resultType="java.util.HashMap">
        SELECT
            p.id          AS playlist_id,
            p.name        AS playlist_name,
            p.description AS description,
            p.created_at  AS created_at,
            COUNT(ps.id)  AS song_count,
            (SELECT ps2.cover_url FROM playlist_song ps2
             WHERE ps2.playlist_id = p.id
             ORDER BY ps2.added_at DESC LIMIT 1) AS cover_url
        FROM playlist p
        LEFT JOIN playlist_song ps ON ps.playlist_id = p.id
        WHERE p.user_id = #{userId}
        GROUP BY p.id
        ORDER BY p.created_at DESC
    </select>

</mapper>
