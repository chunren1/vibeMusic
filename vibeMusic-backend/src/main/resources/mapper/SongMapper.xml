<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vibemusic.mapper.SongMapper">

    <!--
        随机获取 N 首已下载歌曲（应用层随机 OFFSET）
        避免 ORDER BY RAND() 的全表排序开销
    -->
    <select id="findRandomSongs" resultType="com.vibemusic.entity.Song">
        SELECT * FROM song LIMIT #{count} OFFSET #{offset}
    </select>

    <!--
        ON DUPLICATE KEY UPDATE 入库：避免先 SELECT 再 INSERT/UPDATE
        命中的唯一键: uk_source_id (source_id)
    -->
    <insert id="insertOrUpdateUrl">
        INSERT INTO song(source_id, name, artist, album, cover_url, duration, url)
        VALUES(#{sourceId}, #{name}, #{artist}, #{album}, #{coverUrl}, #{duration}, #{url})
        ON DUPLICATE KEY UPDATE
            url = VALUES(url),
            name = VALUES(name),
            artist = VALUES(artist),
            album = VALUES(album),
            cover_url = VALUES(cover_url),
            duration = VALUES(duration)
    </insert>

</mapper>
