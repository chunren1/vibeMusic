# 2026-06-10

## cpolar 监控 SendKey 配置 (16:07)
- start-cpolar-monitor.bat 写入真实 SendKey: SCT360080TS4RhHIAils91EzFHwpqlaaEa
- 启动时自动设置 SCKEY 环境变量，Python 脚本通过 os.environ 读取
- 双击 bat 即可启动，无需手动设置环境变量

## init.sql 完整建表脚本 (16:10)
更新 `vibeMusic-backend/sql/init.sql`，合并 migration_profile.sql 改动：
- users: 补充 bg_image(VARCHAR 500), gender(VARCHAR 10), birthday(VARCHAR 10)
- users: avatar 从 VARCHAR(200) 扩至 VARCHAR(500)
- song: 新增 updated_at 字段（Song extends BaseEntity 需要此字段）
- play_history: 联合索引 idx_user_played(user_id, played_at) 加速推荐查询
- 所有表/字段添加 COMMENT 注释
- 已删除旧的 migration_profile.sql（内容已合并进 init.sql）

